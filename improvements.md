# rag-demo 项目改进需求清单

> 只描述**要求**与**预期效果**,不含实现方案。
> 范围遵循已定结论:不 agent 化、不追生产级完备性(认证/告警/反馈闭环不在本清单),所有改进均不动现有 13 节点架构。

**优先级定义**
- **P0** — 补上"证明"的缺口,直接决定简历条目能否写数字
- **P1** — 已识别的设计缺口加固,消除潜在事故/劣化路径
- **P2** — 顺手项,低成本锦上添花

---

## P0-1 生成端评估体系(质量闭环的另一半)

### 背景
现有 eval 体系只覆盖检索侧(9 腿 × hit/recall/ndcg/mrr)。检索好 ≠ 答案好,幻觉率、答非所问、引用错误都测不到。有检索评估的候选人常见,有生成评估的罕见。

### 要求
1. **覆盖三项核心指标**(RAGAS 或自建 LLM-as-judge 均可,选一即可):
   - **faithfulness(忠实度)**:答案中每个论断能否被检索上下文支撑,输出"支撑率"
   - **answer relevance(答案相关性)**:答案是否回应了原问题(而非子查询/改写后的问题)
   - **引用准确率**:答案中标注的引用编号,指向的 chunk 是否真的支撑对应论断
2. **复用现有 golden 数据集**(`retrieval_golden.jsonl` 的 query 侧),不另建数据集;答案由现有流水线真实生成
3. **与检索 eval 同等地位**:结果落 history 留档、出终端汇总表、可纳入 baseline 门禁
4. **judge 失败要有兜底**:单条评测失败跳过并记录,不拖垮整轮
5. 每轮报告 answer 维度与 retrieval 维度**同屏对照**,能看出"检索好了答案是否跟着好"

### 预期效果
- 简历多一条:**"端到端质量闭环:检索侧 9 腿 × 4 指标 + 生成侧忠实度/相关性/引用准确率,双向门禁"**——这是完整度上压过绝大多数个人项目的一句
- 能回答面试必问的"你怎么知道答案没有幻觉",且答案是一个体系而不是一句话
- 改 prompt/换模型时,生成侧有了回归防线,改坏能被发现

---

## P0-2 压测与成本基线(简历数字的来源)

### 背景
所有简历条目目前零数字。"有数字和没数字的条目,说服力差一个量级"。跑一次,永久受益。

### 要求
1. **延迟指标**:P50/P95/P99 首个 token 延迟(SSE 场景的关键体验指标)、P95 全程完成时间;区分三条路径分别测:**缓存命中 / 单分支直答 / 拆解多分支**
2. **吞吐指标**:单实例可稳定承载的并发用户数与 QPS,记录拐点(延迟开始发散的并发数)
3. **成本指标**:每查询平均 token 成本(输入+输出分开计),以及:
   - 语义缓存命中率 → 折算"节省了多少次生成调用/多少成本"
   - rerank 开启/关闭的延迟差与 recall 差(与 eval 数据联算)→ "用 X ms 延迟换 Y 点 recall"
4. **结果落档**:数字写入文档(含测试环境说明:并发数、模型、池配置),可复现
5. 压测时**顺带验证降级链**:kill 掉 neo4j/reranker 后系统仍出完整回答且延迟可控

### 预期效果
- 简历条目全部升级为量化形态,例如:"P95 首 token 延迟 XXXms"、"语义缓存命中率 XX%,节省 XX% 生成成本"、"rerank 以 XXms 延迟代价换取 recall@5 +X 点"
- 面试谈性能时有真实拐点和数字可讲,而不是"应该挺快的"
- 压测大概率会先暴露 embedding 并发限流(429)和 pg 池排队,为 P1-3 提供实证

---

## P1-1 语义缓存 scope 隔离(隐私缺陷修复)

### 背景
当前语义缓存是 global scope:A 用户的问题可命中 B 用户写入的缓存答案,等价于跨用户信息泄漏。单用户演示无害,多用户即事故。这是全项目唯一"不能只说不修"的缺口。

### 要求
1. 缓存查找与写入的匹配范围必须限定在**同一 scope**(用户或会话级,按项目的部署形态选)
2. 相似度匹配在同一 scope 内进行,**绝不跨 scope 返回缓存**
3. 现有命中率不能因隔离而崩塌:同用户重复提问必须仍能命中
4. 有测试覆盖:两个不同 scope 发相似问题,不得互相命中

### 预期效果
- 消除唯一的隐私级缺陷,项目可安全演示给多人
- 面试叙事从"我知道这是缺口"升级为"我修了,方案是 X"——更硬

---

## P1-2 上下文体积护栏(防 prompt 爆炸)

### 背景
链路中存在两条无 token 级约束的膨胀路径:
- `fuse_multi_query_results` 故意不截断(合理),但若 `RERANK_DYNAMIC_TOPK_ENABLED` 关闭,最多 20 条 chunk 直接进 expand,×3 膨胀 = **60 个 chunk 文本进 prompt**
- 法规 KB 的父文档展开拉**整篇原文**,仅有"≤5 篇"的间接约束,无长度上限

### 要求
1. **fuse 层软上限**:跨子查询融合结果保留 RRF 得分前 N 条(N≈10),多分支高分 chunk 不受损,下游关闭 dynamic_topk 也不失控
2. **generate 前 token 硬上限**:拼接上下文超过预算时,按相关度序截断(保留引用编号连续性),超限事件记日志
3. **父文档展开长度约束**:单篇父文档超长时截断到可配长度,或首尾保留+中部摘要
4. 截断行为**只影响超限场景**:正常路径的上下文逐字节不变,有测试保证

### 预期效果
- 上下文长度上限从"取决于运气"变为"恒定有界",最坏路径从 60 chunk 收敛到预算内
- 多分支 + 大文档场景的首 token 延迟可控,不再随拆分数量线性恶化
- 面试可讲:"检索扇出影响查全率,上下文长度由我显式定界"——分层设计补上了最后一环

---

## P1-3 并发防护(扇出 × 并发用户的乘积)

### 背景
单请求扇出:4 分支 × 2 pg 腿 = 8 个并发 pg 查询,池上限 `PG_POOL_MAX=10` 单请求刚好塞下;**2 个并发用户即超池排队**,5 个并发即大面积尾延迟恶化。另有 4 路并发 embedding 调用,会先于 pg 池撞 DashScope 限流(而 429 在现有重试策略中是**不可重试**的)。

### 要求
1. pg 池上限按压测(P0-2)实测拐点调整,并记录"池大小 ↔ 并发用户数"的容量对应关系
2. **全局并发闸**:对 `search()` 级别设置同时在飞上限,超出排队而非打爆下游——同时覆盖 pg 腿与 embedding 腿
3. embedding 限流(429)被闸住后不升级为用户可见失败
4. 排队要有超时兜底:等待过久的分支降级为空结果,走现有 `no_results`/部分结果路径,不让整图挂死
5. 压测复测:并发用户数达到设计容量时,P95 延迟无发散性恶化

### 预期效果
- 系统容量从"1 个用户完美、2 个用户劣化"变为有明确设计容量且过载时优雅降级
- 与压测数字(P0-2)互为印证,简历可写"单实例设计并发 XX,P95 延迟 XXms"
- 面试可讲 fan-out 架构的通用问题(单请求扇出 × 并发请求 = 下游连接风暴)与自己的解法

---

## P2-1 eval 门禁接入 CI

### 要求
- 每次 MR 自动触发:单元测试 + 检索 eval gate + 生成 eval(P0-1 完成后纳入)
- 指标退化超阈值(现有 >3% 规则)即 CI 失败,block 合并
- 基线更新(`--update-baseline`)仍为手动显式操作,CI 内禁止隐式更新

### 预期效果
- "质量回归门禁是 CI 的一环"成为事实而非口头描述
- 简历工程化条目升级:个人项目做到 CI 级质量拦截,罕见

---

## P2-2 缓存命中可观测

### 要求
- 语义缓存命中率、命中/未命中的相似度分布,进现有可观测体系(Langfuse 或日志聚合计数)
- 未命中但相似度接近阈值的 query 单独可查——它们是阈值调优的直接依据

### 预期效果
- 缓存阈值从拍脑袋变为有数据支撑的调参
- 为 P0-2 的"缓存节省成本"数字提供长期数据源,而非一次性压测快照

---

## 明确不做的(决策记录,防反复讨论)

| 项 | 不做的理由 |
|----|-----------|
| agent 化 / tool use 循环 | 固定流水线对单跳问答更优:低延迟、可预测、**可离线评估**(eval 体系的存在依赖链路确定性)。"评估过、结论是不需要"本身是面试素材 |
| 认证鉴权 / 检索层 ACL | 生产级完备性,展示价值低;以"我清楚离生产还差什么"的叙事带过 |
| 告警 / 反馈闭环 | 同上,琐碎且低简历回报 |
| 流式 mid-stream 重试 | 语义复杂(已发出的 token 如何处理),有意延后——保持现状即可 |

---

## 完成度自检

P0 两项完成后,项目达到"高完成度项目"层次(架构 + 检索评估 + 生成评估 + 量化数字),为该定位下的完满状态;P1 全部完成后,已识别的设计缺口(缓存泄漏、上下文无界、并发风暴)清零;P2 为加分项,时间富余再做。
